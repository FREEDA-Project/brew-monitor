% Deployment
deployedTo(aggregator, tiny, worker1).
deployedTo(data_gather_olbia, tiny, worker1).
deployedTo(data_gather_torino, tiny, worker1).
deployedTo(gateway, tiny, m2).

% Components failures


% Nodes failures


